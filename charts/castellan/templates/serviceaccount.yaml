{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "castellan.serviceAccountName" . }}
  labels:
    {{- include "castellan.labels" . | nindent 4 }}
{{- end }}